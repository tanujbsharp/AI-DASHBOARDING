We are going to develop an AI-based dashboarding system on the Elastic Table. So, we know that the Elastic Table cannot be joined, which means that a single normalized table has to be able to create all the reports. So, let's focus on a single report type, which is a table, and a single database, which is a single Elastic Index. And let's focus on Training Taken Report as a single Elastic Index. Now, you have the Training Taken Report. It has got various fields. Let us take the headers of the fields with sample data for, you know, a couple of rows of it, with detailed explanation, maybe a couple of lines of explanation for each column, which basically explains what that field is all about. And this is fed into Bedrock and hence into Cloud for the basic data. Then we are basically being able to create a table. For creating a table, we will be sending a prompt, right? And that prompt will basically be able to write the SQL or the Elastic Query for the same. In the table, we'll have to figure out from Zoho reports or wherever it is, what constitutes a good table and what constitutes a good table-based report. It might be things like, what is the filter for the table? It might be things like, I want trainings completed last month. That's a good filter for the table. So, that is the context of the table. So, what is the mega filter? Second, what are the filters I want to apply for the table? Which means I might have things like type of training or region of people or whatever it is. And the filters can also always come from the fields which are there at that particular index of Elastic. The third aspect of a table is basically what are the fields which are there in the table in a columnar faction. What is the first field, second field, third field? Maybe I'll start off by saying number, name, email ID, telephone number, course completed, date, points, whatever that is required. So, how will I have an easy way of selecting the fields? And these fields ideally has to come only from the Elastic table. So, we should be able to select the fields which are there. So, in effect, and then I press the submit. I have a very detailed requirement as to what is the nature of the table. What is the specific query I'm going to write? Because I've described the table very nicely in my initial prompt, the query will have a reasonable chance of getting a good answer. And then I'll also have what are the filters which have to be passed. And I'll also have the table with the sequence. Now, the next idea to make this easier is to have a prompt playground in which there is a playground which has selected a specific table, which is, you know, course completion table or whatever, and I have to show all the fields which are there in it. I can sort of select the fields which I want, just drag and drop. And it will sort of take that as the input to write the query. Because I'll be able to generate the query with a lot of finesse and hence have a very good chance of getting the answers correct. So the first step is to actually go through Zoho reports or anything else and understand how exactly is a good table fought. What are the filters? What are the other things? How's the query written? How's the overquery? How are the tables? How are the sortings of the rows done? If you understand that, then you can take access to one elastic team, which is a course completion report in QA. And or you can have it in your client and have it in your client, and that might be a better way. And use that to create this entire engine like a prompt playground. And the prompt playgrounds output will be the table with the filters in the top report with the title of the report, which basically says, you know, course completion in October or whatever you have. So this can be the approach for the AI reports.
 